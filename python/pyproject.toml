[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.11.8",
    "codetiming>=1.4.0",
    "requests>=2.32.3",
]

[tool.pyright]
  venvPath = "."
  venv = ".venv"
